module Plasmacore

class ViewTransform
  GLOBAL METHODS
    method pop
      Canvas.render_queue.write( RenderCmd.POP_VIEW_TRANSFORM )

    method push( m:Matrix )
      Canvas.render_queue.write( RenderCmd.PUSH_VIEW_TRANSFORM )
      Canvas.render_queue.write( m )
endClass

